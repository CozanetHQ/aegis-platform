{
  "name": "aegis-platform",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "pnpm --filter platform dev",
    "build": "pnpm --filter platform build",
    "lint": "pnpm -r lint"
  }
}
